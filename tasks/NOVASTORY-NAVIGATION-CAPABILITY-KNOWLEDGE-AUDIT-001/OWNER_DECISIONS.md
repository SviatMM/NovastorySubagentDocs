# Owner Decisions Relevant to the Map

## Accepted/current routing decisions

- AI launcher = Chats + Projects only; no global Agents.
- AgentTemplate = clean reusable definition; ProjectAgent = Project/domain stateful identity.
- Add Agent = Project → Agents → Add Agent.
- Project workspace = Overview / Conversations / Agents / Repositories / Instructions.
- many Project sources + zero/one primary; bounded local implementation exists.
- standalone Conversation does not require synthetic Project.
- model/Power/Fast/permission are distinct; no silent fallback; live discovery controls availability.
- default-deny typed host boundary; prompt/UI label cannot widen authority.
- NovaStory owns durable product state/authority; connected engine owns reasoning/orchestration.
- future child subagents stage evidence; ProjectAgent is single final publisher for domain.
- local-first foundation remains; device grants/secrets separate.
- Supabase selected only as future small cloud component; Google Drive deferred.
- cloud primary/recovery direction is not automatic failover.
- website is public presentation/download path, not browser NovaStory.
- admin is future service-operator concept only.
- current frontend basis Light/Dark; full-program Glass and NovaStory Island are separate post-1.0 open goals.

## Important open U-IDs
`U-01` ProjectSource lifecycle; `U-02` data placement; `U-03` account/Auth/profile/sync; `U-04` module/plugin allocation; `U-06` provider/runtime expansion; `U-07` Agent library/instructions/memory/materials/publication; `U-08` approval/effects; `U-09` Automation engine; `U-14` Knowledge; `U-15` multi-window; `U-18` second computer; `U-19` NovaStoryAgent/Subagent methodology; `U-20` Island; `U-22` full Glass; `U-23` 0.2.0 wording; `U-24` isolated Test1.

## Post-commit Owner intent

2026-09-14 direct project conversation adds:
1. desired future NovaStory login choices Google, GitHub and Apple plus Apple credential/password-saving UX;
2. exploratory `Smart Subagent` entry under Settings → Connections & Devices with a scripted external ChatGPT-window prototype idea.

These are `OWNER_STATED_POST_COMMIT`: they may change target but do not change audited code or verification. Integrate into canonical Owner Logic/decisions in a separate docs step before implementation.