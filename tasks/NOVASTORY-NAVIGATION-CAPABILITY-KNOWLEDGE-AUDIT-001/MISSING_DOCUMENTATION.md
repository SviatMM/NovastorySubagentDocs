# Missing Documentation / Mapping Gaps

These gaps do not authorize implementation.

| Gap | Risk | Recommended documentation treatment |
|---|---|---|
| no canonical AI-principal→NovaStory-operation exposure matrix | UI/host actions can be misread as model tools | when tools are admitted, add principal/tool exposure fields to existing operation/security catalogs |
| no executable ProjectAgent current-state node | identity-live vs Mission-missing spread across docs | extend existing current-state/coverage route when execution starts |
| no runtime-oriented minimal retrieval router | Agent may read whole vault | use thin derived indexes over HOME/MOCs, not copied truth |
| no Agent result-search/Result Store contract | “find previous result” ambiguous | define with future ResultDestination/Reference implementation |
| no general effect/audit ledger | “everything logged” overclaim | preserve bounded-audit warning until implemented |
| no Mail connector contract | Mail UI looks nearly ready | admit connector semantics before OAuth/API implementation docs |
| no Calendar connector contract | local event UI looks real | same |
| no persisted Automation graph/execution contract | builder mistaken for working automation | resolve U-09 in existing automation/backend catalogs |
| no final account/Auth contract in audited commit | Account popover/cloud docs confuse status | integrate newer Owner login intent into Owner Logic/U-03 before C2 |
| no Smart Subagent canonical entry | latest discussion can be mistaken for feature | integrate Owner intent and classify concept before code |
| no second-Mac runner transport/enrollment | remote authority risk | resolve U-18 in execution/security catalogs |
| no durable Knowledge implementation/index | concept can be mistaken for storage | do not hide implementation inside Chat search |
| no Tasks/Goals runtime model | roadmap intent only | resolve ownership/schema/authority in existing routes |
| no definitive Agent instruction authority | Project/template/file/AgentInstructionSet may conflict | resolve under U-07 |
| no permanent Chat purge contract | live Delete is Trash | keep purge distinct until admitted |
| no remote ProjectSource contract | Repositories can be mistaken for GitHub remote integration | extend only after U-01 |
| no remotely observable local worktree status | audit cannot prove unpushed state | local agent must separately record `git status` evidence |

Not missing: main HOME/INDEX/MOCs, Owner Logic, backend process/operation/data/security catalogs, Chat/Project current-state, frontend matrix/guide, cloud docs route and unresolved decision registry. The dominant problem is routing/freshness, not lack of prose.